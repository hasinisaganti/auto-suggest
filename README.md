# auto-suggest

# this is my main repo for project!
# yo man
# first we have to create a repository in the git hub
# then in vs code we have to clone
# do add a comment in the README.md and save it
# then u will see the changes in the extension click it 
# click the + button and t then in the text box enter name for the commit
# and  then then click commit and sync changes
# then it would ask for the authorization do them.
# then u could see the changes made in the the github repo readme changes
# 2. type npm init -y in the terminal
# and then a package.json will be created. and then change the 
# main :index.js to server .js and then create a file named as server.js
# in terminal npm install express
# then we write the express code and run it in a port
# then we create a frontend folder in auto suggest.
# in that frontend folder create html,css,index files and save them and run them.
# then do npm install -g nodemon in the terminal(this nodemon will work like a live server).
# run nodemon server.js
# 3. we have changed the port in the server.js
# the render shared in the group we have authorized it 
# and then we have changed 2 commands in the build and start command column
# and then deploy it and copy the link and keep it in the git about settings.

